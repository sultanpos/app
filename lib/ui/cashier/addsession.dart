import 'package:flutter/material.dart';
import 'package:sultanpos/ui/widget/basewindow.dart';

class CashierAddSessionWidget extends StatelessWidget {
  const CashierAddSessionWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const BaseWindowWidget(
      title: 'Tambah sesi kasir',
      width: 500,
      height: 300,
      child: Column(),
    );
  }
}

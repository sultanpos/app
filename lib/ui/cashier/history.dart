import 'package:flutter/material.dart';

class CashierHistoryWidget extends StatelessWidget {
  const CashierHistoryWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
